import { heroui } from '@heroui/theme'

export default heroui({
  themes: {
    light: {
      colors: {
        primary: { DEFAULT: '#FF6B1A', foreground: '#FFFFFF' },
        secondary: { DEFAULT: '#0A2E6E', foreground: '#FFFFFF' },
        focus: '#FF6B1A',
      },
    },
  },
})
